---
name: 数据校验
description: 对结构化记录做 schema 与约束校验，标记脏数据
version: 1.0.0
type: atomic
---
# 数据校验

对结构化记录做 schema 与约束校验，标记脏数据

> 这是「技能舰队」demo 的一个原子叶子技能（atomic leaf），由所属领域编排器派发。
> 运行时它产出一次终态结果，触发一条 SkillExecutionRecord 战绩记录。

<<ROUTE:data-validate>>
